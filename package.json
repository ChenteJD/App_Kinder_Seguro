{
  "name": "vinculo-preescolar-monorepo",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm run dev",
    "seed": "cd backend && npm run seed",
    "install:all": "cd backend && npm install"
  }
}